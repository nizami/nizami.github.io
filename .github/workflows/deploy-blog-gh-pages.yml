name: deploy-blog

on:
  push:
    branches: [master]

jobs:
  deploy-blog:
    runs-on: ubuntu-latest
    name: deploy-blog
    steps:
      - uses: actions/checkout@v2
        with:
          path: master

      - name: Install dependencies
        run: ls && cd master && ls && npm ci

      - name: Build
        run: npm run build && ls

      # - uses: actions/checkout@v2
      #   with:
      #     path: gh-pages
      #     ref: gh-pages

      # - name: Setup Node.js
      #   uses: actions/setup-node@v2
      #   with:
      #     node-version: '12'

      # - name: Install dependencies
      #   run: npm ci
