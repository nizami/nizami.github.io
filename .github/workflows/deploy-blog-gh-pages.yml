name: deploy-blog

on:
  push:
    branches: [master]

jobs:
  deploy-blog:
    runs-on: ubuntu-latest
    name: deploy-blog
    steps:
      - uses: actions/checkout@v2
        with:
          ref: master
          path: master

      - name: ls - 1
        run: ls

      - name: cd master
        run: cd master

      - name: ls - 2
        run: ls

      - name: Install dependencies
        run: npm ci

      - name: ls - 3
        run: ls

      - name: Build
        run: npm run build

      - name: ls - 4
        run: ls

      # - uses: actions/checkout@v2
      #   with:
      #     path: gh-pages
      #     ref: gh-pages

      # - name: Setup Node.js
      #   uses: actions/setup-node@v2
      #   with:
      #     node-version: '12'

      # - name: Install dependencies
      #   run: npm ci
